name: stock_trader_pro
description: "Professional Stock Trading Application"
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # HTTP & API
  http: ^1.2.0
  dio: ^5.4.0

  # State Management
  provider: ^6.1.1

  # Charts
  fl_chart: ^0.66.2
  syncfusion_flutter_charts: ^24.1.41

  # UI Components
  google_fonts: ^6.1.0
  shimmer: ^3.0.0

  # Local Storage
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Utils
  intl: ^0.19.0
  url_launcher: ^6.2.2

  # Real-time data
  web_socket_channel: ^2.4.0

  # AI Analysis
  flutter_dotenv: ^5.1.0

  # Supabase
  supabase_flutter: ^2.3.0

  # Notifications
  flutter_local_notifications: ^17.0.0
  dart_openai: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true

  assets:
    - assets/
    - .env
